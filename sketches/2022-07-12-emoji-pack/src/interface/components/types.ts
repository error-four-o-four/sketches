export type Where = Parameters<HTMLElement['insertAdjacentElement']>[0];

// const AsyncFunction = (async () => { }).constructor;

// function isAsyncFunction(fn: unknown): fn is typeof AsyncFunction {
// 	return fn instanceof AsyncFunction;
// }
